public class Decide {
    public String DECIDE(int NUMPOINTS, double[] POINTS, Parameters PARAMETERS, Connector[][] LCM, boolean[] PUV) {
        String LAUNCH = "NO";

        // Calculations ...

        return LAUNCH;
    }
}

enum Connector {
    ANDD,
    ORR,
    NOTUSED
}